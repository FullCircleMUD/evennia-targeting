# SPDX-License-Identifier: BSD-3-Clause
"""Helpers — content-filtering and resolution helpers built on predicates.

Thin wrappers over ``caller.search`` that pre-filter candidate lists
with predicates and delegate string matching to Evennia. Helpers are
added only when a real consumer needs them.

Scaffold: no helpers yet. The first helpers land alongside the first
predicates that justify them.
"""
