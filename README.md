# evennia-targeting
Composable targeting predicates and content-filtering helpers for Evennia. Layers on top of caller.search() to express runtime-state filters (caller identity, mixin-based visibility, lock checks) that Evennia's native search kwargs can't.
